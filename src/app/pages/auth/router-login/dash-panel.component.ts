import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-dash-panel',
  templateUrl: './dash-panel.component.html',
  standalone: true,
  imports: [RouterModule],
})
export default class DashPanelComponent {}
